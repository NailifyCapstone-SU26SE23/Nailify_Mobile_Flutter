# Scratchpad: Global SignalR Notification Listener Integration in Mobile FE

## Current Goal
Bỏ hiển thị trùng lặp (Dialog Popup + SnackBar Banner) khi Salon từ chối hoặc báo giá mẫu nail custom.

## Progress / Completed Steps
- [x] Tạo `rootNavigatorKey` toàn cục trong `app_router.dart` và gán vào `GoRouter(navigatorKey: rootNavigatorKey)`.
- [x] Tạo component `GlobalSignalRListener` (`lib/core/widgets/global_signalr_listener.dart`) quản lý toàn bộ SignalR streams.
- [x] Nhúng `GlobalSignalRListener` vào `MaterialApp.router` (thông qua thuộc tính `builder`) trong `main.dart`.
- [x] Loại bỏ gọi trùng lặp `_showStyledSnackBar` trong `_customNailQuotedSub` và `_customNailRejectedSub`, chỉ giữ lại Dialog Popup trung tâm duy nhất.
- [x] Kiểm tra lại bằng `flutter analyze`: Phản hồi **No issues found!**

## Key Decisions Made
- `CUSTOM_NAIL_QUOTED` và `CUSTOM_NAIL_REJECTED` sẽ hiển thị duy nhất 1 Dialog Popup đẹp mắt ở trung tâm màn hình thay vì vừa hiển thị Dialog vừa hiển thị SnackBar ở dưới.

## Remaining TODOs
- [x] Đã hoàn thành sửa trùng lặp.
