# Agent Scratchpad

## Current Goal
Add visible UI entry points for Snapshot Try-On feature in `home_page.dart` and `main_shell.dart`.

## Progress / Completed Steps
- [x] Initial codebase exploration and comparison of both projects
- [x] User alignment via `/grill-me` (architecture, folder structure, navigation flow)
- [x] Created `implementation_plan.md` artifact and obtained user approval
- [x] Copied ONNX asset files (`best.onnx`, `thanhdtPose.onnx`, `ar.onnx`, `best.pt`) to `assets/models/`
- [x] Updated `pubspec.yaml` with required dependencies (`onnxruntime`, `image`, `http`) and asset path
- [x] Copied all Dart AI modules (`ai/*`, `isolate/*`, `painter/*`, `models/*`, `services/*`, `presentation/nail_snapshot_page.dart`) to `lib/features/try-on/`
- [x] Updated `TryOnMethodSelectionScreen` to launch `NailSnapshotPage` directly
- [x] Fixed Android Gradle duplicate native library build error (`MergeNativeLibsTask`) via `packaging.jniLibs.pickFirsts` in `android/app/build.gradle.kts`
- [x] Added `hand_landmarker: ^3.0.1` to `pubspec.yaml`.
- [x] Added `androidResources { noCompress += listOf("onnx", "pt", "task", "tflite", "json") }` to `android/app/build.gradle.kts`
- [x] Added direct GoRouter path `/snapshot-try-on` in `lib/core/routing/app_router.dart`.
- [x] Added `HomeSnapshotTryOnBanner` on `home_page.dart` and camera icon shortcut on `main_shell.dart` header.

## Key Decisions Made
- Add a dedicated banner on the Home page and camera shortcut in header to make Snapshot Try-on immediately visible to the user.

## Remaining TODOs
- [x] Complete guidance response.
